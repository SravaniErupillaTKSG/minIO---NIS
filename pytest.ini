[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (require live MinIO)
    unit: marks tests as unit tests (use mocks)
log_cli = true
log_cli_level = INFO
